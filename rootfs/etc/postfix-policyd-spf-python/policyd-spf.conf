# Emit RFC 8601 results under the same receiver identity as OpenDKIM and
# OpenDMARC. Sender-provided authentication verdicts are removed before these
# local checks run.
Header_Type = AR
Authserv_Id = mailcue
Hide_Receiver = No
